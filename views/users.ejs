<%- include('includes/head.ejs') %>

</head>

<body>
    <h1>Users</h1>
    <% if(users.length > 0) {%>
        <ul>
            <% users.forEach((user) => { %>
                <li><%= user.name %></li>
            
            <% }) %> 
        </ul>
    <%} else { %>
       <h1>No users</h1> 
    <% } %>
</body>

<%- include('includes/end.ejs') %>